{
  "name": "proxy-wrapper",
  "version": "1.0.0",
  "description": "SOCKS5 proxy wrapper with TCP stack tuning for OS fingerprint spoofing",
  "main": "src/index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "socks": "^2.8.3"
  }
}
